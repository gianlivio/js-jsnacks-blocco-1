for (let i = 0; Math.pow(2, i) <= 1000; i++) {
    console.log(Math.pow(2, i));
}

// Il ciclo for inizia con i = 0 e aumenta i di 1 ad ogni iterazione. La condizione di continuazione del ciclo è che Math.pow(2, i) sia minore o uguale a 1000. Quando Math.pow(2, i) supera 1000, il ciclo si ferma e lo script termina.